/**
 * Shared barrel — export reusable pipes, directives, and components here.
 * Import into any standalone component that needs them.
 *
 * Example:
 *   import { LoadingSpinnerComponent } from '@app/shared';
 */
export * from './components/loading-spinner/loading-spinner.component';
export * from './pipes/distance.pipe';
export * from './pipes/rupee.pipe';
